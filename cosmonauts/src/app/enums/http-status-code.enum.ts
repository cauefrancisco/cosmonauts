export enum EHttpStatusCode {
    OK = 200,
    NO_CONTENT = 204,
    BAD_REQUEST = 400,
    UNAUTHORUZED = 401,
    FORBIDDEN = 4003,
    NOT_FOUND = 4004,
    INTERNAL_SERVER_ERROR = 500,
    BAD_GATEWAY =502,
    SERVICE_UNAVAILABLE = 503,
    GATEWAY_TIMEOUT = 504
}